#pragma once 

enum Face {
    U,
    D,
    F,
    B,
    L,
    R,
    ERROR
};

enum Direction {
    ANTI_CLOCK_WISE = -1,
    CLOCK_WISE = 1
};

enum LocalCoordinate {
    TOP,
    RIGHT,
    DOWN,
    LEFT,
};

enum Color {
    WHITE,
    YELLOW,
    GREEN,
    BLUE,
    ORANGE,
    RED,
};