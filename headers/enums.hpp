#pragma once 

enum Face {
    U,
    D,
    F,
    B,
    L,
    R,
    ERROR
};

enum Direction {
    CLOCK_WISE = -1,
    ANTI_CLOCK_WISE = 1
};

enum LocalCoordinate {
    RIGHT,
    TOP,
    LEFT,
    DOWN,
};

enum Color {
    WHITE,
    YELLOW,
    GREEN,
    BLUE,
    ORANGE,
    RED
};