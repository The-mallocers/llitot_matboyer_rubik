#pragma once 

enum Face {
    U,
    D,
    R,
    L,
    F,
    B,
    ERROR
};

enum Direction {
    CLOCK_WISE,
    ANTI_CLOCK_WISE
};

enum Color {
    WHITE,
    YELLOW,
    BLUE,
    GREEN,
    ORANGE,
    RED
};